# Development dependencies for local testing with full features
#
# Install with: pip install -r requirements-dev.txt
#
# This includes heavy dependencies for optional server-side STL generation
# (does not work on Vercel, but useful for local development/testing)

# Production dependencies
-r requirements.txt

# Heavy geometry processing (optional - for local server-side generation)
numpy==2.4.1
trimesh==4.10.1
shapely==2.1.2
mapbox_earcut==2.0.0
scipy==1.16.3              # For mesh.fix_normals() (fallback: unify_normals)
matplotlib==3.10.8         # For character markers (fallback: rectangles)
manifold3d==3.3.2          # Boolean operations (not available on Vercel)

# Development tools
pytest==9.0.2
pytest-cov==7.0.0
ruff==0.14.10              # Linting and formatting
mypy==1.19.1               # Type checking
pre-commit==4.5.1          # Git hooks
