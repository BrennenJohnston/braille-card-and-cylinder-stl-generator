# Minimal dependencies for Vercel serverless deployment
# Heavy packages (scipy, matplotlib, manifold3d) are excluded because:
# - Client-side CSG handles boolean operations in the browser
# - Server returns 503 for server-side STL generation, prompting client-side fallback
# - Character markers fall back to rectangles without matplotlib
# - Mesh normals use unify_normals() instead of fix_normals() without scipy
#
# For local development with all features, use: pip install -e .
# (This installs from pyproject.toml with all dependencies)

flask==3.1.2
flask-cors==6.0.1
Flask-Limiter==3.8.0
redis==5.0.7
requests==2.32.5
numpy==1.26.4
trimesh==4.5.3
shapely==2.1.2
mapbox_earcut==2.0.0
